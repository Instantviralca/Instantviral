/**
 * Learn Categories & Tags config — Document 15.04.
 */

/** Tag archive pages under `/learn/tag/[tag]`. Prepared for V1; enabled when ready. */
export const LEARN_TAG_PAGES_ENABLED = true;

export const LEARN_TAG_PATH_PREFIX = '/learn/tag' as const;

export const DEFAULT_CATEGORY_PAGE_SIZE = 12;
