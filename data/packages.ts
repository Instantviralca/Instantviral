/**
 * @deprecated Import from `@/data/pricing` instead.
 * Compatibility shim for Document 09 → 10.01 migration.
 */
export {
  getPackagesByServiceSlug,
  getPackageById,
  getPackagesByIds,
  resolveServicePackages,
} from '@/data/pricing/packages';
