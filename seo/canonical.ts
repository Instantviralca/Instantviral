/**
 * Canonical URL helpers — re-export from Metadata Engine (Document 14.07).
 */

export {
  absoluteUrl,
  buildCanonicalUrl,
  canonicalUrl,
  getSiteOrigin,
  normalizeCanonicalPath,
} from '@/lib/seo/metadata/canonical';
